[project]
name = "file_sync_example"
version = "0.1.0"
description = "A minimal example library for file synchronization"
requires-python = ">=3.8"
dependencies = ["etiket_client"]

[build-system]
requires = ["setuptools", "packaging"]
build-backend = "setuptools.build_meta"
